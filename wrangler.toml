name = "bchydro-proxy"
main = "worker.js"
compatibility_date = "2025-12-06"

# Environment variables (non-sensitive)
# These can be set in Cloudflare Dashboard: Settings > Variables
[vars]
CACHE_MAX_AGE = "300"  # Cache duration in seconds (default: 5 minutes)
