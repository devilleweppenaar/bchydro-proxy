name = "bchydro-proxy"
main = "src/worker.js"
compatibility_date = "2025-07-18"

[build]
command = "gleam build"

# Environment variables (non-sensitive)
[vars]
CACHE_MAX_AGE = "300"
TEST_MODE = "false"

# Named environments (optional, for explicit deployment targets)
[env.production]
vars = { CACHE_MAX_AGE = "300", TEST_MODE = "false" }

[env.development]
vars = { CACHE_MAX_AGE = "60", TEST_MODE = "true" }
