name = "bchydro-proxy"
main = "src/index.js"
compatibility_date = "2025-07-18"

# Environment variables (non-sensitive)
# These can be set in Cloudflare Dashboard: Settings > Variables
[env.production]
vars = { CACHE_MAX_AGE = "300" }

[env.development]
vars = { CACHE_MAX_AGE = "60" }
